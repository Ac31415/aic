if a recording session has more than 1 episodes, say 2 episodes, when i pressed the right arrow key to get to the next episode again i'd get prompted to enter passwords again. it seems like it is entering the docker container again through distrobox on another terminal/shell session. make it so that when moving from one episode to the next, do control c on the current distrobox session (basically ending the gazebo scene), and then immediately relaunch using the `/entrypoint.sh` command followed by another randomized arguments based on the current dataset task in the same current distrobox sesssion.